<%= form_for(@nation_group) do |f| %>
  <% if @nation_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nation_group.errors.count, "error") %> prohibited this nation_group from being saved:</h2>

      <ul>
      <% @nation_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
