<h1>Listing payment_groups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payment_groups.each do |payment_group| %>
  <tr>
    <td><%= link_to 'Show', payment_group %></td>
    <td><%= link_to 'Edit', edit_payment_group_path(payment_group) %></td>
    <td><%= link_to 'Destroy', payment_group, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment group', new_payment_group_path %>
