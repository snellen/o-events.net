<%= form_for(@payment_group) do |f| %>
  <% if @payment_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_group.errors.count, "error") %> prohibited this payment_group from being saved:</h2>

      <ul>
      <% @payment_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
