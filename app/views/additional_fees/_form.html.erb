<%= form_for(@additional_fee) do |f| %>
  <% if @additional_fee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additional_fee.errors.count, "error") %> prohibited this additional_fee from being saved:</h2>

      <ul>
      <% @additional_fee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
