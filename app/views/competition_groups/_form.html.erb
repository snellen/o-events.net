<%= form_for(@competition_group) do |f| %>
  <% if @competition_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competition_group.errors.count, "error") %> prohibited this competition_group from being saved:</h2>

      <ul>
      <% @competition_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
